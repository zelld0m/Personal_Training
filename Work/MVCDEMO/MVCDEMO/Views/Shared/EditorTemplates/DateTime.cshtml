@model DateTime?
    @Html.TextBox("", Model.HasValue ? ViewData.TemplateInfo.FormattedModelValue : "", new { @class="date"})

                                                            @*//  YES,  NO*@ 